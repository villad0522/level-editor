{
  "name": "level-editor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server",
    "build": "npm run build:1 && npm run build:2",
    "build:1": "vite build --ssrManifest --outDir dist/client",
    "build:2": "vite build --ssr src/server/entry-server.js --outDir dist/server",
    "preview": "cross-env NODE_ENV=production node server"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "sass": "^1.69.7",
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "@types/wicg-file-system-access": "^2023.10.4",
    "bootstrap": "^5.3.2",
    "compression": "^1.7.4",
    "constrained-editor-plugin": "^1.3.0",
    "express": "^4.18.2",
    "monaco-editor": "^0.45.0",
    "sirv": "^2.0.4"
  }
}
